<?php
/**
 * Created by PhpStorm.
 * 阿里云OSS配置
 * User: fer
 * Date: 2016/11/15
 * Time: 18:14
 */
date_default_timezone_set('PRC');

$dateTxt = date('Y-m-d H:i:s', time());
/*
|-----------------------------------------
| 提供阿里云OSS操作所需使用的参数
|-----------------------------------------
*/
return [
    /*
    |--------------------------------------------------------------------------
    | OSS上传对象
    |--------------------------------------------------------------------------
    */
    'object_path'    => env('OSS_OBJECT_PATH'),

    /*
    |--------------------------------------------------------------------------
    | 文件读取根路径
    |--------------------------------------------------------------------------
    */
    'read_file_path' => env('READ_FILE_PATH'),

    /*
    |--------------------------------------------------------------------------
    | 图片水印
    |--------------------------------------------------------------------------
    */
    'watermark'      => env('WATERMARK'),

    /*
    |--------------------------------------------------------------------------
    | OSS上传对象
    |--------------------------------------------------------------------------
    */
    'oss_bucket'     => env('OSS_BUCKET'),

    'private_bucket' => env('OSS_PRIVATE_BUCKET'),

    /*
    |--------------------------------------------------------------------------
    | OSS连接信息 ID KEY URL
    |--------------------------------------------------------------------------
    */
    'oss_cons_info'  => [
        'access_id'  => 'TJRpuASj4TrZ1MPd',
        'access_key' => 'JAeF8EKZ1m6IqDc9d3lycWs4kwqgcX',
        'hostname'   => env('OSS_URL', 'sp-public.oss-cn-qingdao-internal.aliyuncs.com'),
    ],

    /*
    |--------------------------------------------------------------------------
    | OSS上传单个图片时的头信息
    |--------------------------------------------------------------------------
    */
    'options'        => [
        'headers' => [
            'Expires'             => $dateTxt,
            'Cache-Control'       => $dateTxt,
            //'Content-Disposition' => 'just-for-test',
            'Content-Encoding'    => 'utf-8',
        ],
    ],
    'signTime'       => $dateTxt,
];
